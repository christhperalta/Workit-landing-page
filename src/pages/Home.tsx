import { Footer, Header, Main } from "../components";

export const Home = () => {
  return (
    <div style={{ overflowX: "hidden" }}>
      <Header />
      <Main />
      <Footer />
    </div>
  );
};

export default Home;
